//alive = 0 + 3 ^ 3
//not_alive = alive + 2
//array: [] = [ alive, not_alive ]
//lol: [] = [ alive, not_alive ]

plus_one(x) = x + 1
plus_three(x) = plus_one(x) + 2
inc!(x, y) = x -> plus_one(x), y -> x
join(a: [], b: []) -> [] = a | b